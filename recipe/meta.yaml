{% set name = "openmeeg" %}
{% set version = "2.4.4" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  # url: https://github.com/{{ name }}/{{ name }}/archive/{{ version }}.tar.gz
  url: https://github.com/larsoner/openmeeg/archive/0ca2d5e63d1c37e4b0ad85561d23e05debc8053a.tar.gz
  sha256: 36c73a12803c638642dd9cc0e7c2de4fdc83e77bba91b507ca422937271e48c5

# https://conda-forge.org/docs/maintainer/knowledge_base.html
build:
  number: 0

requirements:
  build:
    - python                                 # [build_platform != target_platform]
    - cross-python_{{ target_platform }}     # [build_platform != target_platform]
    - numpy                                  # [build_platform != target_platform]
    - cmake >=3.8
    - make
    - ninja
    - {{ compiler('cxx') }}
    - llvm-openmp  # [osx]
    - libgomp      # [linux]
    - pthread-stubs  # [win]
    - swig

  host:
    - python
    - numpy
    - hdf5  # [win]
    - hdf5-static  # [not win]
    - openblas
    - libgfortran  # [not win]
    - zlib
    - libmatio

  run:
    - python
    - {{ pin_compatible('numpy') }}
    - libgfortran  # [not win]
    - zlib
    - libmatio

test:
  imports:
    - openmeeg
  commands:
    - python -c "import openmeeg; assert openmeeg.sqr(4) == 16"

about:
  home: https://openmeeg.github.io/
  license: CECILL-B
  license_file: LICENSE.txt
  summary: >
           A C++ package for solving the forward problems of
           electroencephalography (EEG) and magnetoencephalography (MEG).

extra:
  recipe-maintainers:
    - massich
    - agramfort
    - larsoner
