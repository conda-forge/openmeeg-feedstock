{% set version = "2.4.1" %}

package:
  name: openmeeg
  version: {{ version }}

source:
  # TODO: PR and merge https://github.com/larsoner/openmeeg/commits/conda-forge
  url: https://github.com/larsoner/openmeeg/archive/149c581ed11f69e90c5e7ad5d16c66bd03a8a2f0.tar.gz
  sha256: 7460e69d0f66df3104f1c0200b65bae204b60a9f7207e469f8c0ad1ca2d06437
  # patches:
  #   - overwrite_opnmeegpy_installation_path_win.patch  # [win]

# https://conda-forge.org/docs/maintainer/knowledge_base.html
build:
  number: 201
  skip: true  # [win and vc<14]

requirements:
  build:
    - python                                 # [build_platform != target_platform]
    - cross-python_{{ target_platform }}     # [build_platform != target_platform]
    - numpy                                  # [build_platform != target_platform]
    - swig                                   # [build_platform != target_platform]
    - cmake >=3.8
    - make
    - ninja
    - {{ compiler('cxx') }}
    - llvm-openmp  # [osx]
    - libgomp      # [linux]

  host:
    - python
    - numpy
    - pthread-stubs  # [win]
    - openblas
    - libblas
    - libcblas
    - liblapack
    - liblapacke
    - swig
    - hdf5
    - zlib
    - libmatio

  run:
    - python
    - {{ pin_compatible('numpy') }}
    - libblas * *openblas
    - openblas
    - hdf5
    - zlib
    - libmatio

test:
  imports:
    - openmeeg
  commands:
    - python -c "import openmeeg; assert openmeeg.sqr(4) == 16"

about:
  home: https://openmeeg.github.io/
  license: CECILL-B
  license_file: LICENSE.txt
  summary: >
           A C++ package for solving the forward problems of
           electroencephalography (EEG) and magnetoencephalography (MEG).

extra:
  recipe-maintainers:
    - massich
    - agramfort
    - larsoner
