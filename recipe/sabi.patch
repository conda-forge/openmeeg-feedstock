diff --git a/wrapping/python/CMakeLists.txt b/wrapping/python/CMakeLists.txt
index 4e89b3fb..9f72ba71 100644
--- a/wrapping/python/CMakeLists.txt
+++ b/wrapping/python/CMakeLists.txt
@@ -15,7 +15,8 @@ if (CMAKE_VERSION VERSION_GREATER_EQUAL "3.27")
     cmake_policy(SET CMP0148 NEW)  # fail FindPythonInterp / FindPythonLibs
 endif()
 
-find_package(Python3 REQUIRED COMPONENTS Interpreter NumPy)
+find_package(Python 3 REQUIRED COMPONENTS Interpreter NumPy Development.SABIModule)
+add_library (Python::Module ALIAS Python::SABIModule)
 
 if (BUILD_TESTING)
     find_package(Pytest)
@@ -24,8 +25,8 @@ endif()
 # XXXX: this include_directories should be removed and transitive properties
 #       of OpenMEEG::OpenMEEG and OpenMEEG::OpenMEEGMaths should be used instead
 
-include_directories(${Python3_INCLUDE_DIRS}
-                    ${Python3_NumPy_INCLUDE_DIRS}
+include_directories(${Python_INCLUDE_DIRS}
+                    ${Python_NumPy_INCLUDE_DIRS}
                     ${CMAKE_CURRENT_SOURCE_DIR}
                     ${PROJECT_SOURCE_DIR}/OpenMEEG/include/
                     ${PROJECT_SOURCE_DIR}/OpenMEEGMaths/include/
@@ -64,27 +65,27 @@ endif()
 if (PYTHON_FORCE_EXT_SUFFIX)
     # For PyPy, see
     # https://github.com/conda-forge/openmeeg-feedstock/pull/20#issuecomment-1161729565
-    if(NOT Python3_EXT_SUFFIX)
+    if(NOT Python_EXT_SUFFIX)
         if(UNIX)
-            set(Python3_EXT_SUFFIX ".${Python3_SOABI}.so")
+            set(Python_EXT_SUFFIX ".${Python_SOABI}.so")
         else()
-            set(Python3_EXT_SUFFIX ".${Python3_SOABI}.pyd")
+            set(Python_EXT_SUFFIX ".${Python_SOABI}.pyd")
         endif()
     endif()
 
-    set_target_properties(openmeeg PROPERTIES SUFFIX "${Python3_EXT_SUFFIX}")
-    message("Python forced suffix ${Python3_EXT_SUFFIX}")
+    set_target_properties(openmeeg PROPERTIES SUFFIX "${Python_EXT_SUFFIX}")
+    message("Python forced suffix ${Python_EXT_SUFFIX}")
 else()
-    message("Python default suffix ${Python3_EXT_SUFFIX}")
+    message("Python default suffix ${Python_EXT_SUFFIX}")
 endif()
 
 if (APPLE)
     swig_link_libraries(${SWIG_TARGET_NAME} OpenMEEG::OpenMEEG)
 else()
-    swig_link_libraries(${SWIG_TARGET_NAME} ${Python3_LIBRARIES} OpenMEEG::OpenMEEG)
+    swig_link_libraries(${SWIG_TARGET_NAME} ${Python_LIBRARIES} OpenMEEG::OpenMEEG)
 endif()
 
-set(PYTHON_SITE_ARCH ${Python3_SITEARCH})
+set(PYTHON_SITE_ARCH ${Python_SITEARCH})
 if (PYTHON_INSTALL_RELATIVE)
     string(REGEX MATCH "[Ll]ib.*packages" PYTHON_SITE_ARCH ${PYTHON_SITE_ARCH})
 endif()
@@ -115,8 +116,8 @@ add_custom_command(
 # You need to be in the CMAKE_SOURCE_DIR for python -m setuptools_scm -c wrapping/python/pyproject.toml
 # to properly detect and use .git_archival.txt
 
-execute_process(COMMAND ${Python3_EXECUTABLE} -m setuptools_scm -c wrapping/python/pyproject.toml
-                WORKING_DIRECTORY ${CMAKE_SOURCE_DIR} 
+execute_process(COMMAND ${Python_EXECUTABLE} -m setuptools_scm -c wrapping/python/pyproject.toml
+                WORKING_DIRECTORY ${CMAKE_SOURCE_DIR}
                 OUTPUT_VARIABLE PYTHON_OPENMEEG_VERSION
                 OUTPUT_STRIP_TRAILING_WHITESPACE
                 COMMAND_ERROR_IS_FATAL LAST)
@@ -126,8 +127,8 @@ message("Python Q: Generating ${CMAKE_CURRENT_BINARY_DIR}/openmeeg-${PYTHON_OPEN
 add_custom_command(
     TARGET ${SWIG_TARGET_NAME} PRE_BUILD
     WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR}
-    COMMAND ${CMAKE_COMMAND} -E env SETUPTOOLS_SCM_PRETEND_VERSION=${PYTHON_OPENMEEG_VERSION} PYTHON=${Python3_EXECUTABLE}
-                                    SDIR=${CMAKE_CURRENT_SOURCE_DIR} BDIR=${CMAKE_CURRENT_BINARY_DIR} -- 
+    COMMAND ${CMAKE_COMMAND} -E env SETUPTOOLS_SCM_PRETEND_VERSION=${PYTHON_OPENMEEG_VERSION} PYTHON=${Python_EXECUTABLE}
+                                    SDIR=${CMAKE_CURRENT_SOURCE_DIR} BDIR=${CMAKE_CURRENT_BINARY_DIR} --
                              ${CMAKE_COMMAND} -P ${CMAKE_CURRENT_SOURCE_DIR}/create_dist_info.cmake
     VERBATIM
 )
@@ -175,7 +176,7 @@ if (BUILD_TESTING)
     endif()
     set(PYTHON_TEST_FILES test_vector.py test_matrix.py test_sensors.py test_geometry.py test_python.py test_mesh.py)
     foreach (test ${PYTHON_TEST_FILES})
-        add_test(openmeeg_python_${test} ${Python3_EXECUTABLE} -m pytest --tb=short -ra ${TEST_PYTHON_ARGS} -vv ${OpenMEEG_BINARY_DIR}/wrapping/python/openmeeg/tests/${test})
+        add_test(openmeeg_python_${test} ${Python_EXECUTABLE} -m pytest --tb=short -ra ${TEST_PYTHON_ARGS} -vv ${OpenMEEG_BINARY_DIR}/wrapping/python/openmeeg/tests/${test})
         if (USE_MKL AND BUILD_SHARED_LIBS AND NOT MKL_USE_sdl)
             # due to this bug in the MKL
             message(WARNING "Due to a bug in the MKL when used from python: see https://software.intel.com/en-us/forums/intel-distribution-for-python/topic/628976 "
@@ -188,7 +189,7 @@ if (BUILD_TESTING)
     endforeach()
 
     if (BUILD_REFERENCE_DOC)
-        add_test(openmeeg_python_doc ${Python3_EXECUTABLE} ${OpenMEEG_BINARY_DIR}/wrapping/python/openmeeg/tests/test_doc.py)
+        add_test(openmeeg_python_doc ${Python_EXECUTABLE} ${OpenMEEG_BINARY_DIR}/wrapping/python/openmeeg/tests/test_doc.py)
         set_tests_properties(openmeeg_python_doc PROPERTIES ENVIRONMENT PYTHONPATH=${OpenMEEG_BINARY_DIR}/wrapping/python)
     endif()
 
